---
# ── WORK CASE STUDY TEMPLATE ─────────────────────────────────────────────────
# Duplicate this file and drop the leading underscore; name it for the slug:
#     cp _template.mdx new-project.mdx
# Files starting with "_" never publish. The build rejects invalid frontmatter
# and tells you exactly what's wrong.
# ─────────────────────────────────────────────────────────────────────────────

title: "Project title — outcome-led, in sentence case"   # required
# slug: "custom-slug"                                     # optional — defaults to filename
description: "150–160 chars. Meta description + index summary. Be specific."  # required
client: "Client / practice name"                          # required
sector: "cosmetic-surgery"                                # required — cosmetic-surgery | medical-aesthetics | dermatology | dental | wellness
services: ["Brand identity", "Web design", "SEO"]         # required — at least one
year: "2026"                                              # required
publishedAt: "2026-01-01"                                 # required — YYYY-MM-DD (controls ordering)
# updatedAt: "2026-01-02"                                 # optional
# location: "Sydney, AU"                                  # optional
# url: "https://client-site.com"                          # optional — live site link
summary: "One-line outcome shown on the work index card." # optional but recommended
featured: true                                            # optional — surface on the homepage
# metrics: headline outcome stats (optional, up to 3 shown)
metrics:
  - value: "+212%"
    label: "Organic enquiries, six months"
  - value: "0.9s"
    label: "Largest Contentful Paint"
  - value: "#1"
    label: "Local ranking, primary procedure"
# testimonial (optional): the client's REAL words — never draft these for them.
# Renders as a centred quote band near the foot of the detail page.
# testimonial:
#   quote: "One or two sentences from the client."
#   author: "Dr Jane Smith"
#   role: "Cosmetic doctor, Sydney"

# Images (optional). If set, the *Alt field is REQUIRED (accessibility).
# heroImage fills the detail page's hero BrowserMockup (~16:10 desktop capture);
# mobileImage is the PhoneMockup overlapping its corner (tall mobile capture);
# domain labels the mockup's address bar (fm.url's host used when omitted).
# heroImage: "/assets/desktops/your-project.jpg"
# heroImageAlt: "Plain description of the hero image"
# mobileImage: "/assets/phones/your-project.jpg"
# mobileImageAlt: "Plain description of the mobile capture"
# domain: "client-domain.com"
# thumbImage: "/assets/desktops/your-project.jpg"
# thumbImageAlt: "Plain description of the thumbnail"
---

Open with the problem the client arrived with, in plain language.

## The brief

What they actually needed — the underlying goal, not just the deliverable.

## What we did

How we approached it. A short list works well:

- First thing we did
- Second thing we did
- Third thing we did

> A pull-quote that captures the thinking behind the work.

## The result

What changed. Lead with the outcome that mattered most to the client, and keep it honest.
