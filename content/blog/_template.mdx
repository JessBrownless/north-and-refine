---
# ── JOURNAL ENTRY TEMPLATE ───────────────────────────────────────────────────
# Duplicate this file and drop the leading underscore; name it for the slug:
#     cp _template.mdx my-post.mdx
# Files starting with "_" never publish. The build rejects invalid frontmatter.
# ─────────────────────────────────────────────────────────────────────────────

title: "Your post title in sentence case"                # required
# slug: "custom-slug"                                     # optional — defaults to filename
description: "One or two sentences, ~150–160 chars. Meta description + social preview." # required
publishedAt: "2026-01-01"                                 # required — YYYY-MM-DD
# updatedAt: "2026-01-02"                                 # optional
category: "seo"                                           # required — seo | web-design | branding | conversion | studio-notes
# author: "North & Refine"                                # optional — defaults to the studio
# readingTime: 4                                          # optional — auto-computed if omitted

# Featured image (optional; featuredImageAlt REQUIRED if featuredImage is set):
# featuredImage: "/assets/journal/your-image.avif"
# featuredImageAlt: "Plain description for screen readers"
# featuredImageCaption: "Optional visible caption"
---

Open with a plain paragraph that sets up the piece.

## A section heading

Body copy. Headings get clear space above them automatically.

### A smaller sub-heading

More copy beneath a sub-heading.

A short list:

- First point
- Second point
- Third point

> A pull-quote worth emphasising.

A closing paragraph to round it off.
